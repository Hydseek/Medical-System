<div id="notifications-widget" class="dropdown notifications position-relative">
    <a href="#" id="notifications-toggle" class="nav-link d-inline-block position-relative" aria-expanded="false">
        <i class="fas fa-bell"></i>
        <span id="notifications-count" class="badge bg-danger rounded-pill position-absolute top-0 start-100 translate-middle">0</span>
    </a>

    <div id="notifications-list" class="card dropdown-menu p-0 shadow-lg" style="display:none; min-width: 320px; right: 0; left: auto;">
        <div class="card-header d-flex justify-content-between align-items-center">
            <strong class="mb-0">Notifications</strong>
            <button id="mark-all-read" class="btn btn-sm btn-link">Mark all read</button>
        </div>

        <ul id="notifications-items" class="list-group list-group-flush small">
            <li class="list-group-item text-muted empty">No notifications</li>
        </ul>

        <div class="card-footer text-center small text-muted">
            <a href="#" id="notifications-view-all">View all</a>
        </div>
    </div>
</div>
<script src="{{ asset('js/notifications.js') }}"></script>